import QtQuick 1.1


Rectangle {

    // AC controls styles
    property int controlsColumnWidth: Math.round(AcWindow.childAt(centerColumnTop).width / 3)
    property int controlsColumnHeight: Math.round(AcWindow.childAt(centerColumnTop).height)
    property int age: 33
}
